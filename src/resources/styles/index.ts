import theme from '../theme';

const styles = {
  container: {
    flex: 1,
    padding: 16,
    backgroundColor: theme.colors.background,
  },
  mt16: {
    marginTop: 16,
  },
};

export default styles;
